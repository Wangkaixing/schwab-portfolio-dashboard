# 持仓分析看板便携版

## 使用要求

电脑需要安装 Node.js 22 或更高版本。首次启动需要联网安装运行依赖，完成后日常使用不需要公网服务。

## macOS

1. 解压整个压缩包。
2. 双击 `启动看板-macOS.command`。
3. 首次启动会自动安装依赖，随后浏览器自动打开看板。
4. 如果系统阻止首次运行，请右键脚本并选择“打开”。

## Windows

1. 解压整个压缩包。
2. 双击 `启动看板-Windows.bat`。
3. 首次启动会自动安装依赖，随后浏览器自动打开看板。

## 停止看板

关闭启动脚本对应的终端窗口即可。看板只监听本机地址 `127.0.0.1`，局域网或公网中的其他设备无法访问。

## 迁移交易数据

维护数据保存在原电脑浏览器中，不会自动包含在便携包里：

1. 在原电脑点击“导出 JSON”。
2. 将导出的 JSON 和便携包复制到新电脑。
3. 在新电脑启动看板，点击“更新 JSON”导入。

请不要直接移动或删除项目中的文件。需要迁移时，应复制完整的解压目录。
